spring:
  application:
    name: microserver-config-repo
  cloud:
    config:
      uri: http://localhost:8086/
      profile: default
      label: master
      enabled: true

management:
  security:
    enabled: false